const homeView = "/"
const productsView = "/products"
const signInView = "/signin"
const brandsView = "/brands"
const newsView = "/news"

export default{
  homeView,
  productsView,
  brandsView,
  newsView,
  signInView
}